import { HStack, Text } from "@chakra-ui/react";

const Logo = () => {
  return (
    <HStack>
      <Text fontSize={"large"} fontWeight={"bold"}>
        CollectionHub
      </Text>
    </HStack>
  );
};

export default Logo;
