import { HStack, Text } from "@chakra-ui/react";

const Logo = () => {
  return (
    <HStack>
      <Text fontSize={20} fontWeight={"bold"}>
        CollectionHub
      </Text>
    </HStack>
  );
};

export default Logo;
